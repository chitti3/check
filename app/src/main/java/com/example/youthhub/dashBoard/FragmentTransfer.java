package com.example.youthhub.dashBoard;

import android.support.v4.app.Fragment;

public interface FragmentTransfer {

    void fragmentTransferListener(Fragment fragment);

    void hideSearchView(boolean hideSearch);

}
